component: backbone
kind: patch
custom:
  category: Fixed
  pr: ""
body: >
  `pkit upgrade` no longer crashes when started with no stdin or a closed one
  (as under some process supervisors); it treats that as non-interactive and
  prints the tool-update instruction instead of self-updating (#913).
