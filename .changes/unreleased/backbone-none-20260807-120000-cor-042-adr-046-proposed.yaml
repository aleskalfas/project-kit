component: backbone
kind: none
custom:
  category: Changed
body: >
  Author COR-042 (process health: opt-in hand-off contracts + report-only
  missed-hand-off check) and ADR-046 (resolve-predicate seam, sibling-module
  placement) as proposed (#609). Design-ahead: no surface change until the
  pair is accepted; implementation (#610) and the COR-038/COR-035 amendment
  notes land with the acceptance change-set.
