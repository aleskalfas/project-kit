component: project-management
kind: patch
custom:
  category: Fixed
body: >
  `done-work` now transitions the issue right after the squash-merge and treats
  every local branch step as a warning, so a worktree on a detached HEAD or one
  whose `main` is checked out elsewhere no longer reports a landed merge as an
  error and no longer leaves the issue stranded at Review (#878).
