# Changesets

Pending version-intent declarations live in `unreleased/` until a release
consumes them (PRJ-002). See `.pkit/release/README.md` for the workflow.
changie config is `.changie.yaml`; a changeset is equally hand-writable.
