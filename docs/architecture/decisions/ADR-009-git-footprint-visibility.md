---
id: ADR-009
title: Adopter-controlled git footprint — private visibility via per-clone exclude, with a confirm-gated untrack
status: accepted
date: 2026-06-04
author: Ales Kalfas <kalfas.ales@gmail.com>
---

> **Amendment 1 (2026-06-21) — accepted.** This record's original v1 (status `accepted`, above) deferred a pkit-owned ignore file for the handful of inherently-local runtime files under `.pkit/` (rule 6; *Alternatives*, second bullet) per COR-007. That deferral is now **lifted**: the runtime-local set recurred (PRJ-006's diagnose capture-log + TTL marker, joining `sandbox-provenance.yaml` and the `__pycache__/` caches), earning the carrier. The amendment **pins a pkit-owned `.pkit/.gitignore`** rendered wholesale from a per-component `runtime_ignore:` declaration. It **extends** v1 — every original decision (private via `info/exclude`, no committed *root* `.gitignore`, the footprint seam, the untrack posture) stands unchanged — so it lands in place rather than as a successor record (per `.pkit/decisions/README.md`, "Refining an accepted record"). The amendment is marked throughout with **[Amendment 1]**; it is accepted and the v1 body around it remains in effect. See the **Amendment 1** section after *Implications*.

## Context

[COR-002](../../../.pkit/decisions/core/COR-002-merge-delivery.md) established how pkit delivers content into *fixed-path adopter-owned* config files: a merge primitive that contributes, never subtracts. The complementary rule is the **no-shared-files invariant** ([COR-001](../../../.pkit/decisions/core/COR-001-content-mechanisms.md) / [COR-017](../../../.pkit/decisions/core/COR-017-capability-pattern.md)): every file has exactly one owner — pkit owns its files outright and never co-edits a file the adopter owns. Today pkit owns no part of any `.gitignore`; `install.py` merely *prints* a recommended snippet, and project-kit's own root `.gitignore` is hand-curated. pkit's standing disposition toward adopter git state is **print, don't mutate**.

A real adoption scenario is unserved: a developer wants to use pkit on a repo whose **other members have not adopted it** — so pkit's footprint (`.pkit/`, the deployed `.claude/skills` + `.claude/agents`) must stay **git-private**: working locally, invisible to the shared tree, forcing nothing on teammates who never chose it. The default — what project-kit itself is — commits pkit so a whole team shares one methodology.

A `critic` + `architect` consult and an audit of the first draft settled the shape. Decisive findings: (1) **markers inside a committed `.gitignore` are out** — COR-002 explicitly *rejected* in-file markers under the no-shared-files invariant; pkit must not co-edit the adopter's shared `.gitignore`. (2) A `.gitignore` cannot hide *its own* directory, so hiding the whole `.pkit/` folder requires a *parent*-level rule; **`.git/info/exclude`** — a per-clone, never-committed ignore file — is the only native channel that can hide `.pkit/` while leaving zero committed trace. (3) `git rm --cached` is a **new git-index-mutation posture** for the backbone and must be deliberate. (4) The footprint spans tiers (core `.pkit/`, adapter `.claude/` deploys, capability state), so it must be **declared per component**, never hardcoded in core. (5) `.claude/settings.json` is co-owned and out of scope. (6) The narrow set of *inherently-local runtime files* (e.g. `sandbox-provenance.yaml`) that one might ignore even in shared mode is small and not the stated need — **deferred** per [COR-007](../../../.pkit/decisions/core/COR-007-pattern-extraction.md). Scoping v1 to the private case means **pkit touches no committed `.gitignore` whatsoever** — it writes only the per-clone `.git/info/exclude` — which sidesteps the marker / shared-file question entirely. The product owner directed that going private **always** runs the cleanup, gated by an explanatory confirmation. Proposed status is the acceptance-gate gesture per [PRJ-005](../../../.pkit/decisions/project/PRJ-005-adopt-adrs.md).

## Decision

**pkit's git footprint is adopter-controlled through two visibility modes over a per-component-declared footprint, realized entirely through the per-clone `.git/info/exclude` — no committed `.gitignore` is ever written. `shared` (default) commits pkit and keeps `info/exclude` clear of pkit. `private` writes the whole footprint to `info/exclude` so pkit is invisible to the shared repo, and — because going private means "stop sharing pkit" — always runs a confirm-gated untrack of any footprint files already tracked. No file the adopter owns is ever co-edited.**

1. **The footprint is one declared list per component.** Each installed component (backbone, adapter, capability) declares its footprint in its package metadata: `footprint: [<glob>…]` — the paths that constitute pkit's presence (e.g. backbone `.pkit/`; the claude-code adapter its `.claude/skills`, `.claude/agents`). The mechanism aggregates declarations across installed components. Core owns the mechanism; components contribute their paths (the registration seam the deploy primitives already use). Core never names adapter- or capability-specific paths. No `committed`/`local_only` split — v1 hides the footprint wholesale or not at all.

2. **`shared` (default) — pkit is committed; `info/exclude` is kept clear.** `pkit visibility shared` ensures none of the aggregated footprint is present in `.git/info/exclude` (removing pkit's lines if a prior `private` added them), returning pkit to normal tracked/committable state. It writes **no** `.gitignore` — pkit's files are committed the ordinary way, exactly as project-kit self-hosts today.

3. **`private` — make the whole footprint git-invisible via `.git/info/exclude`.** `pkit visibility private` writes the aggregated footprint (including `.pkit/` itself and the `.claude/` deploys) to `.git/info/exclude`. This is the only native channel that (a) sits in a *parent* context, so it can hide the `.pkit/` directory a nested `.gitignore` cannot hide itself, and (b) is never committed, so privacy leaves no trace in the shared repo. A fresh private install therefore never tracks pkit and needs no cleanup. (Its usual downside — per-clone, lost on re-clone — is a feature here: pkit re-writes it on install, and you *want* it invisible and unshared.) pkit owns a delimited region *within* `info/exclude` (a file git itself owns, not the adopter — markers here co-edit nothing the adopter authored), so it can add and later remove its own lines idempotently without disturbing any hand-added exclude entries.

4. **Going private always runs a confirm-gated untrack.** Because switching to private *means* "remove pkit from the shared repo," `visibility private` always invokes the untrack step: it computes `git ls-files ∩ footprint`, and if any footprint files are already tracked, presents an **explanatory confirmation** — naming that these files will be removed from the shared tree on the next commit, while local working copies are preserved (`git rm --cached` keeps the file on disk) — and on confirmation runs `git rm --cached` on exactly those paths. On a fresh install the set is empty and the step is a silent no-op. `--dry-run` previews both the `info/exclude` write and the untrack set, mutating nothing. Declining leaves the exclude rules written and the files tracked (re-runnable later).

5. **`untrack` is also a standalone, footprint-restricted, precondition-guarded verb.** `pkit visibility untrack [--dry-run]` runs the same confirm-gated action independently. (Implemented as a subcommand under `visibility` rather than a bare top-level verb — it lives in the footprint domain and reads consistently with the noun-first command grammar, while remaining its *own* verb, never silently folded into a `shared`/`private` mode flip; the quarantine the consult asked for is preserved.) It **only ever** touches paths in the declared footprint, never stages a non-pkit file, and preserves working copies. It **refuses when the repo is mid-operation** (merge / rebase / cherry-pick in progress) or when target footprint paths have staged changes, rather than entangle its removal with a pending index. This is the deliberate new git-index-mutation posture, bounded by: footprint-only, confirmation-required, `--dry-run`-previewable, working-copy-preserving, clean-precondition.

6. **`.claude/settings.json` and inherently-local runtime files are out of v1 scope.** `settings.json` is co-owned (harness baseline + pkit's merged additions + the operator's own settings) and cannot be ignored or untracked as a unit; pkit's per-machine realized enforcement state belongs in the harness's already-gitignored `.claude/settings.local.json` (a related locality fix, tracked separately). The handful of runtime-local files under `.pkit/` (e.g. `sandbox-provenance.yaml`, caches) that a *shared*-mode install might want ignored are **deferred** (COR-007): until then they simply show as untracked, which is harmless. **[Amendment 1 lifts this deferral — see the *Amendment 1* section: these are now delivered via a pkit-owned `.pkit/.gitignore`, not left untracked.]**

## Rationale

**Why `.git/info/exclude` and nothing committed.** The privacy requirement is that the shared repo show *no sign* of pkit. The only git-native ignore channel that is itself uncommitted is `.git/info/exclude`; using it means pkit hides its whole footprint — including the `.pkit/` directory, which a nested `.gitignore` cannot hide itself — while writing nothing into any file the team shares. Because v1 does not attempt shared-mode ignoring at all, pkit never co-edits the adopter's root `.gitignore`, so COR-002's marker rejection and the no-shared-files invariant are honored trivially rather than navigated. The delimited region pkit owns lives only inside `info/exclude`, a file git owns — co-editing nothing the adopter authored.

**Why the shared/private asymmetry is forced, not arbitrary.** A `.gitignore` can never hide its own directory — only a parent can. Shared mode needs no ignore at all (pkit is committed); private mode must hide `.pkit/` *itself*, which requires a parent-level, per-clone, uncommitted rule. `info/exclude` is the one channel that satisfies all three, and the intent is *personal* (only this clone), which is exactly what `info/exclude` is for in git's own design.

**Why going-private bundles untrack, against the consult's default to split.** The consult recommended quarantining the destructive untrack into its own verb so a policy toggle never silently stages deletions. The product-owner refinement keeps the safety property the consult cared about — *no silent destruction* — via the explanatory confirmation, while honoring that "go private" *means* "stop sharing pkit," of which removing already-shared files is an inseparable part. `info/exclude`-for-private makes this safe in the common case: a fresh install never tracks pkit, so untrack is a genuine no-op, firing only when files were already committed — precisely when cleanup is wanted. The standalone `untrack` verb still exists (rule 5).

**Why a declared footprint rather than a hardcoded list.** Hardcoding in core would make core depend on the claude-code adapter's deploy paths (a layering inversion) and silently miss a second adapter's or a capability's footprint. Declaration-and-aggregation is the same registration seam the deploy primitives use, and keeps the mechanism harness-neutral ([COR-014](../../../.pkit/decisions/core/COR-014-universal-applicability.md)) while each tier owns the truth about its own files.

**Why an ADR and not a new COR.** This mechanism composes only existing principles — the native `.git/info/exclude` channel, the registration-seam pattern, the no-shared-files invariant honored by writing nothing committed, and a bounded new git gesture. It introduces no new universal principle; it is project-kit's architectural realization, matching the ADR-002…008 hybrid. Should a second visibility surface appear later (e.g. shared-mode local-only ignoring, or controlling whether decision records sync to a remote), the broader *adoption-visibility* idea would have earned its own COR; one mechanism is not yet a principle (COR-007).

### Alternatives considered

- **A marker-delimited managed region in the committed root `.gitignore`.** Rejected — co-owns a file the adopter owns, which COR-002 rejected under the no-shared-files invariant. Scoping v1 to private (which uses only the uncommitted `info/exclude`) removes the need to write any committed `.gitignore`.
- **A pkit-owned `.pkit/.gitignore` for shared-mode local-only files now.** Deferred (COR-007) — the set is tiny (`sandbox-provenance.yaml`, caches), it is not the stated need, and keeping it out lets v1 touch no committed file at all. Revisit when the local-only set proves worth managing. **[Amendment 1: the set proved worth managing; this alternative is now adopted — see the *Amendment 1* section.]**
- **A `.gitignore` (or marker block) for private mode instead of `info/exclude`.** Rejected — a committed file can't hide `.pkit/` without a committed trace that leaks pkit's presence; and a nested `.pkit/.gitignore` cannot hide its own parent. `info/exclude` is the only parent-level *and* uncommitted channel.
- **A single `visibility` verb that silently untracks.** Rejected — silent destruction behind a policy flip. The confirmation (rule 4) is what makes bundling acceptable.
- **Hardcoded footprint list in core.** Rejected — layering inversion; misses second-adapter / capability footprints.
- **Print-only untrack.** Rejected by product-owner direction in favor of pkit executing it, gated by confirmation — `--dry-run` output *is* the inspectable printed-commands form.
- **`git update-index --skip-worktree`.** Rejected — files stay tracked-in-tree, visible to teammates; fails the privacy need.
- **A new COR for adoption-visibility.** Deferred — see rationale.

## Implications

- **New package-metadata `footprint` block (registration seam).** Backbone, adapters, and capabilities gain an optional `footprint: [<glob>…]` declaration. Minimal schema growth — visibility *modes* are policy the mechanism applies, not component data. A pure addition; confirm via `pkit migrations check-diff` it trips no COR-010 migration.
- **New CLI surface (PRJ-002 bump).** `pkit visibility` (no subcommand = status) with `shared` / `private` / `untrack` subcommands, each `--dry-run`-able. New commands are a surface change → version bump.
- **New, bounded git-index-mutation posture.** `visibility untrack` is the first backbone gesture that mutates adopter git-index state. Bounded by rule 5. Recorded here so the boundary cannot later widen by accident.
- **Self-hosting + migration — trivially clean.** pkit writes no committed `.gitignore`; project-kit self-hosts in `shared` mode with its hand-curated root `.gitignore` entirely untouched. No migration question arises.
- **Docs.** `install.py`'s print-a-suggestion gitignore guidance + the adapter README should note the new `visibility` control. The architecture docs gain this ADR.
- **Build sanction on acceptance.** Accepting this record sanctions building the footprint declaration + aggregation, the `private` / `shared` `.git/info/exclude` writer (delimited pkit region, idempotent add/remove), the confirm-gated `untrack` (bundled into `visibility private` and standalone, with the clean-precondition guard), and `--dry-run` — with tests, README rows, and a version bump.

## Amendment 1 — runtime-local ignore lines ship via a pkit-owned `.pkit/.gitignore`

*Status: accepted (2026-06-21). The v1 body above remains accepted and in effect; this section adds a mechanism v1 explicitly deferred, and changes none of v1's standing decisions.*

**In one line:** the inherently-local runtime files under `.pkit/` are now actually git-ignored, by a `.gitignore` that **pkit owns and pkit alone writes** — sitting *inside* `.pkit/`, rendered from scratch on every install, never co-edited with anything the adopter authored. v1 deferred this and left those files merely untracked; the set has since recurred, so it earns the carrier.

**Why now — the deferral's trigger fired.** v1 parked this because the local-only set was a single file (`sandbox-provenance.yaml`) plus caches — too small to justify a mechanism (COR-007's recurrence test). It has since grown a second independent instance: PRJ-006's permission-diagnostic loop adds a capture-log and a TTL armed-marker under `.pkit/permissions/project/`, both inherently per-clone runtime state. PRJ-006 sub-decision 5 ("MVP cadence and guards") promised that log would be git-ignored from the same change-set that introduced capture — and its interim realization hand-edited the adopter's root `.gitignore`. With `sandbox-provenance.yaml`, the `__pycache__/` caches under `.pkit/`, and now the diagnose log + marker, the runtime-local set is a recurring shape across two components, not a one-off. The deferral is **lifted** and the carrier is pinned below.

### A1 — The mechanism: a pkit-owned `.pkit/.gitignore`, rendered wholesale

pkit delivers runtime-local ignore lines through a **`.pkit/.gitignore` that pkit owns outright** — a single-owner file, re-rendered in full on each install/sync from the components' declarations, *not* merged into.

1. **Single owner, so no markers and no merge.** Because pkit owns the whole file, the no-shared-files invariant holds the way it holds for any propagated file: there is exactly one owner, so there are **no marker regions** and **no line-level merge semantics**. The file is **rendered wholesale** — regenerated from the current set of installed components every run, the same posture `.claude/settings.json` is regenerated under (and the authoritative-region delivery COR-002 added per COR-028). This is the property that *dissolves* the problems that killed every merge-into-the-adopter's-file option (see A4): an uninstalled or removed capability's lines are simply absent from the next render — there is no orphaned line to reconcile, and no append-only-vs-adopter-deletion tension, because the adopter never edits this file and pkit never appends to it.

2. **Per-component declaration seam — `runtime_ignore:`.** Components declare their own runtime-local ignore patterns in their `package.yaml`, under a `runtime_ignore:` key sitting beside the `footprint:` key v1 introduced. The mechanism manifest-walks installed components and aggregates these exactly as v1 aggregates `footprint:` and as `merge-settings.sh` aggregates skill-grants / DEC-030 overlays. **Core never invents paths it does not own:** the permissions surface declares its diagnose log + TTL marker (retiring PRJ-006's interim root-`.gitignore` hand-edit); the backbone declares its own caches (`__pycache__/`) and `sandbox-provenance.yaml`'s home; each tier owns the truth about its own runtime files. This reuses v1's registration-seam rationale verbatim — declaration-and-aggregation keeps the mechanism harness-neutral (COR-014) and avoids the layering inversion a hardcoded core list would create.

3. **Committed — hence durable and immediate.** Unlike the per-clone `info/exclude` that v1 uses for *privacy*, `.pkit/.gitignore` is **committed**. It is therefore present from first checkout with no per-clone regeneration window, and — the point — it *actually ignores*: it closes the `git add -A` footgun where the diagnose capture-log (carrying redacted command strings) sat merely untracked and one stray `add -A` would stage it into the shared tree.

4. **Scope is the `.pkit/` subtree.** A nested `.gitignore` governs its own directory and below; all the runtime-local state in scope lives under `.pkit/`, so a `.pkit/.gitignore` is sufficient for this set. The known constraint (the mirror of v1's "a `.gitignore` cannot hide its own parent"): this file **cannot** cover a runtime-local artifact that lands *outside* `.pkit/`. None in today's set do; should one appear, it needs a different carrier and a fresh decision — it does not retroactively justify reaching into the adopter's root `.gitignore`.

### A2 — Why this honours v1's privacy rationale rather than violating it

v1 *rejected* a committed `.gitignore`. That rejection was specifically about **the adopter's root `.gitignore`** — co-editing a file the adopter owns (COR-002's marker rejection) and, for the privacy case, leaving a committed trace that discloses pkit's presence. A committed `.pkit/.gitignore` triggers neither harm, by visibility mode:

- **Shared mode** — `.pkit/` is already committed and visible (it is what project-kit self-hosts). A committed file *inside* an already-committed directory adds **zero new disclosure**: anyone who can see `.pkit/` already sees pkit.
- **Private mode** — v1 routes the *entire* `.pkit/` tree into `.git/info/exclude`, so `.pkit/.gitignore` is excluded along with everything else under `.pkit/`. It is **inert** there: never staged, never committed, no leak.

So the file discloses nothing v1's privacy contract protects: in shared mode there is nothing to hide, and in private mode it is hidden with the rest of `.pkit/`. The "committed `.gitignore` leaks pkit" rejection was never about a file *inside* the visible footprint.

### A3 — Currency note on COR-002's "runtime-state gitignore lines" example *(do not edit COR-002)*

COR-002's merge contract gives, as a **Tier-1 auto-add** example, "runtime-state gitignore lines" (COR-002, the *Apply* tier list). A future reader could mistake that line as a mandate to build the committed-root-`.gitignore` *merge* path for runtime-state. After this amendment, that reading is wrong: **runtime-state ignore lines are delivered through this pkit-owned wholesale-rendered `.pkit/.gitignore`, not through the committed-root-`.gitignore` merge tier.** COR-002's example still correctly illustrates *line-based Tier-1 auto-add in general* (the adopter-owned-root-`.gitignore` merge path remains a valid mechanism for content the adopter genuinely co-owns) — it is just not the carrier for pkit's *own* runtime-state. COR-002 is core-owned and not edited here (per the no-shared-files invariant and rule 4); this note records the currency point so the architectural record stays coherent. Should the divergence prove confusing in practice, the clean resolution is an upstream COR-002 refinement, not an edit from this ADR.

### A4 — Alternatives considered (Amendment 1)

- **Extend `merge-settings.sh` (or a sibling merge primitive, or a generic runtime-merge) to write the adopter's committed root `.gitignore`.** Rejected — all three reopen v1's no-shared-files rejection (co-editing a file the adopter owns) *and* hit an unsolvable line-based-merge problem: append-only merge cannot tell an adopter's intentional deletion of a pkit line from a line pkit never added, and it cannot remove a line orphaned when a capability is uninstalled. Wholesale render of a pkit-*owned* file sidesteps both — there is nothing to merge and nothing to orphan.
- **`.git/info/exclude` (v1's private channel) for runtime-state too.** Rejected — `info/exclude` is per-clone and uncommitted, so it is lost on re-clone and absent on first checkout; runtime-state ignoring wants to be durable and immediate in *shared* mode, which only a committed file gives. (v1's privacy case wants the opposite — uncommitted — which is why the two concerns use two different channels, correctly.)
- **Keep deferring; leave the files untracked.** Rejected — the set recurred (the COR-007 trigger), and "untracked" leaves the `git add -A` footgun live on a log carrying redacted command strings.
- **A root-level `.pkit-runtime.gitignore` or similar.** Rejected — needless new path and naming surface; the nested `.pkit/.gitignore` covers the whole in-scope set and reads as obviously pkit-owned.

### A5 — Implications (Amendment 1)

- **Schema growth — a `runtime_ignore:` key.** `package.yaml` gains an optional `runtime_ignore: [<glob>…]` declaration alongside `footprint:`. A pure addition (new optional key, components opt in); confirm via `pkit migrations check-diff` that it trips no COR-010 migration. The render mechanism and the wholesale-vs-merge posture are mechanism behaviour, not component data — only the path list is declared.
- **A new pkit-owned committed path: `.pkit/.gitignore`.** Owned and wholesale-rendered by pkit; first such file pkit commits on the adopter's behalf inside its own tree. Self-hosting is clean: project-kit renders its own `.pkit/.gitignore` from backbone + permissions declarations; the hand-curated *root* `.gitignore` stays untouched, exactly as v1 left it.
- **Retire PRJ-006's interim root-`.gitignore` hand-edit.** PRJ-006 sub-decision 5 git-ignored the diagnose log via an interim root-`.gitignore` edit; once this mechanism ships, the permissions surface declares those paths in `runtime_ignore:` and the interim hand-edit is removed.
- **Build sanction on acceptance.** Accepting this amendment sanctions building the `runtime_ignore:` declaration + aggregation, the wholesale `.pkit/.gitignore` renderer (regenerate-from-declarations, idempotent, run alongside the existing install/sync render steps), the permissions + backbone declarations, and the retirement of PRJ-006's interim root-`.gitignore` edit — with tests, the schema-doc row, and a version bump (new schema key + new rendered file is a surface change per PRJ-002). **This amendment is `accepted`; the build is sanctioned. The version bump lands with that implementation, not with this decision-record acceptance (mirroring v1, whose bump was tied to the `pkit visibility` CLI surface, not the ADR).**

### A6 — Follow-ups this amendment does not enact

- **PRJ-006 sub-decision 5 wording must be reconciled (separate decision-author task — outside ADR custody).** PRJ-006 sub-decision 5 (and its *Implications* echo) say the diagnose log is "git-ignored through the merge primitive" / "via the merge primitive." After this amendment that wording is inaccurate — the log is git-ignored via the pkit-owned `.pkit/.gitignore` render, *not* the merge primitive. PRJ-006 is a project-namespace record outside this architect's write scope (rule 4); **flagged here for a decision-author follow-up** to refine PRJ-006's two phrases to name this mechanism. (Refinement-in-place per `.pkit/decisions/README.md`, "Refining an accepted record"; no supersession — the substance of sub-decision 5 stands, only the named carrier changes.)
- **Implementation is not authored here.** This is the decision record only. The renderer, the schema key, the declarations, and the PRJ-006 interim-edit retirement are the build work the acceptance sanction (A5) covers.
