schema_version: 1
component:
  kind: capability
  name: cli
  version: 0.1.0
description: A CLI output discipline — commands emit typed data events rendered to two contracts, a stable machine surface and a porcelain human view.
requires_backbone: ">=1.149.0,<2.0.0"
